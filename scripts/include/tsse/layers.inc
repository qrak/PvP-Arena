use basic;
const LAYER_NONE        :=  0;   // Not used?
const LAYER_HAND1       :=  1;   // One handed weapon.
const LAYER_HAND2       :=  2;   // Two handed weapon, shield or misc.
const LAYER_SHOES       :=  3;   // Shoes.
const LAYER_PANTS       :=  4;   // Pants.
const LAYER_SHIRT       :=  5;   // Shirt.
const LAYER_HELM        :=  6;   // Helm or Hat.
const LAYER_GLOVES      :=  7;   // Gloves.
const LAYER_RING        :=  8;   // Ring.
const LAYER_UNUSED9     :=  9;   // Unused.
const LAYER_NECK        :=  10;   // Neck.
const LAYER_HAIR        :=  11;   // Hair
const LAYER_WAIST       :=  12;   // Waist (half apron).
const LAYER_CHEST       :=  13;   // Torso (inner) (chest armor).
const LAYER_WRIST       :=  14;   // Bracelet.
const LAYER_PACK2       :=  15;   // Unused (backpack, but ord. bp is 0x15).
const LAYER_BEARD       :=  16;   // Facial hair.
const LAYER_TUNIC       :=  17;   // Torso (middle) (tunic, sash etc.).
const LAYER_EARS        :=  18;   // Earrings.
const LAYER_ARMS        :=  19;   // Arms.
const LAYER_CAPE        :=  20;   // Back (cloak).
const LAYER_PACK        :=  21;   // Backpack
const LAYER_ROBE        :=  22;   // Torso (outer) (robe)
const LAYER_SKIRT       :=  23;   // Legs (outer) (skirt/robe).
const LAYER_LEGS        :=  24;   // Legs (inner) (leg armor).
const LAYER_MOUNT       :=  25;   // Mount (horse, ostard etc.).
const LAYER_VENDORSTOCK :=  0x1a;   // This vendor will sell and restock.
const LAYER_VENDOREXTRA :=  0x1b;   // This vendor will resell to players but not restock.
const LAYER_VENDORBUY   :=  0x1c;   // This vendor can buy from players but does not stock.
const LAYER_BANKBOX     :=  0x1d;   // Contents of bankbox
const LAYER_TRADE       :=  0x1e;   // Can be multiple of these, do not use directly.
