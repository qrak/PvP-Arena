use basicio;
use uo;
use npc;
use basic;

program can_insert(who, backpack, item, movetype)
  if(who.cmdlevel > 2)
    return 1;
  else
    PrintTextAbovePrivate(backpack, "Nie mozesz trzymac tu przedmiotow.", who);
    return 0;
  endif
endprogram
